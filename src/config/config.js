const config = {
    apiBaseUrl: 'http://localhost:3000',
    defaultPageSize: 100,
    endpoints: {
        sendMail: '/api/send-email',
        // Add other endpoints here as needed
        
    }
};

export default config;